// const express = require('express')
// const router = express.Router({ mergeParams: true });
// const ctr = require('../controllers/invoices')
// const auth = require('../controllers/auth')

// router.get('/', auth.authenticated, auth.isSelf, ctr.getAllTeams)
// router.get('/:teamId', auth.authenticated, auth.isSelf, ctr.getAllMembersAndTasks)
// router.get('/:teamId/users', auth.authenticated, auth.isSelf, ctr.addUserToTeam)
// router.get('/:teamId/users/:userId', auth.authenticated, auth.isSelf, ctr.removeUserFromTeam)

// module.exports = router;